import React, { Component, PropTypes } from 'react';

class Silo extends Component {

    constructor(props, context) {
        super(props, context);
    }

    render() {

        return (
            <div>
                Hey !
            </div>
        )
    }
}

Silo.propTypes = {
};

export default Silo;